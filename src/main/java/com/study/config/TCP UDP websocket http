网络编程模型（客户机/服务器）（C/S）
客户机需要服务时向服务器发出请求，服务器则作为守护进程始终运行，监听网络端口

tcp/ip
面向连接的协议，数据不会丢失，不重复

udp
无连接的协议，数据可能丢失

tcp,udp区别
tcp建立连接后开始通信，udp类似广播，直接进行通信；
tcp只支持点对点的通信，udp支持一对多，多对一，多对多；
tcp是面向字节流的，udp是面向报文的，字节流数据可以拆分发送，报文数据只能一次发送；
tcp首部开销大（20字节），udp（8字节）

tcp，udp应用场景
对实时性要求高的用udp，如游戏，直播，媒体通信
大部分情况http都是用tcp

tcp三次握手
客户端向服务端发送SYN
服务端返回SYN,ACK
客户端发送ACK

socket：
长连接，全双工通信，双向